
export function formatCompany(company) {
    //crear logica de formateo de compañia
    return company;
};

export function formatMember(member) {
    //crear logica de formateo de miembro
    return member;
};

export function formatItem(item) {
    //crear logica de formateo de item
    return item;
};